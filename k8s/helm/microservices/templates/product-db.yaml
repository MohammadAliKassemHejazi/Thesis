apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.productDb.name }}
  labels:
    app: {{ .Values.productDb.name }}
spec:
  ports:
    - port: {{ .Values.productDb.service.port }}
      name: postgres
  selector:
    app: {{ .Values.productDb.name }}
  clusterIP: None  # Headless service for StatefulSet identity

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ .Values.productDb.name }}
spec:
  serviceName: {{ .Values.productDb.name }}
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Values.productDb.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.productDb.name }}
    spec:
      containers:
        - name: postgres
          image: "{{ .Values.productDb.image.repository }}:{{ .Values.productDb.image.tag }}"
          imagePullPolicy: {{ .Values.global.imagePullPolicy }}
          ports:
            - containerPort: 5432
              name: postgres
          env:
            - name: POSTGRES_USER
              value: {{ .Values.productDb.credentials.username }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: product-db-secret
                  key: password
            - name: POSTGRES_DB
              value: {{ .Values.productDb.credentials.database }}
          resources:
            requests:
              cpu: {{ .Values.productDb.resources.requests.cpu }}
              memory: {{ .Values.productDb.resources.requests.memory }}
            limits:
              cpu: {{ .Values.productDb.resources.limits.cpu }}
              memory: {{ .Values.productDb.resources.limits.memory }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - {{ .Values.productDb.credentials.username }}
                - -d
                - {{ .Values.productDb.credentials.database }}
            initialDelaySeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - {{ .Values.productDb.credentials.username }}
                - -d
                - {{ .Values.productDb.credentials.database }}
            initialDelaySeconds: 10
            timeoutSeconds: 5
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        {{- if .Values.productDb.persistence.storageClass }}
        storageClassName: {{ .Values.productDb.persistence.storageClass }}
        {{- end }}
        resources:
          requests:
            storage: {{ .Values.productDb.persistence.size }}
